@model Nuntius.Models.Favourite
 @using Nuntius.Helpers


<div class="container ">
    <br />
    <br />

    @foreach (var item in Model.Articles)
    {
       


            string sourceUrl = item.Url;

        <div class="col-sm-3 col-md-3 col-lg-3">
            <div class="list-group">
                <!--Article Image-->
                <img src="@item.UrlToImage" class="img-responsive" />

                <!--Article Link-->
                <a href="@item.Url" class="list-group-item">
                    <!--Article Title-->
                    <p class="list-group-item-heading"><h3>@item.Title</h3></p>
                    <!--Article Description-->
                    <p class="blockquote-inverse">@item.Description</p>
                    <!--Article Author-->
                    <p class="blockquote-reverse text-muted">Author: @item.Author</p>
                    <p class="blockquote-reverse text-muted">Published: @item.PublishedAt</p>
                    <p class="blockquote-reverse text-muted">Source:</p>
                </a>
               @{

                var lastWord = helperfunctions.hashing(item.Title);


                }
                         <a class="glyphicon-bullhorn" data-id="0" href="@Url.Action("Index", "Article", new { id = lastWord, source = @item.SourceId})">Go to Article</a>

                <a class="glyphicon-bullhorn" data-id="0" href="@Url.Action("Delete", "Favorite", new { id = @item.ArticleId})">Delete from Favorites</a>
               </div>

           
            <hr />
        </div>
                }
</div>